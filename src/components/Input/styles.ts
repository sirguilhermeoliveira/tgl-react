import styled from 'styled-components';

export const Input = styled.input`
  font-style: italic;
  font-weight: bold;
  font-size: 17px;
  color: #9d9d9d;
  border: none;
  border-bottom: 2px solid #ebebeb;
  outline: none;
  padding: 20px;

  &::placeholder {
    color: #9d9d9d;
  }
`;
